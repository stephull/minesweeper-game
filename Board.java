/*
 * Board.java
 * Class for the board, including mines and ability to click
 * on each board-piece, randomization of bomb locations. 
 */

public class Board {
	
	// minimum dimensions for window, assumes easiest setting of game
	static final int MIN_HEIGHT = 0;
	static final int MIN_WIDTH = 0;
	
	// maximum dimensions for window, assumes hardest setting of game
	static final int MAX_HEIGHT = 0;
	static final int MAX_WIDTH = 0;
	
	Board() {
		
	}
}
